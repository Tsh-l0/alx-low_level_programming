#include "main.h"
#include <string.h>

/**
 * _strcmp - Compares two strings
 *
 * @s1: The first string to compare
 * @s2: The second string to compare
 *
 * Return: 1 if s1 if the strings are the same
 * 0 otherwise
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
