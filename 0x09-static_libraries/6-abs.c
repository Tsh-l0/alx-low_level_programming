#include "main.h"
#include <stdlib.h>

/**
 * _abs - Find the absolute value of an integer
 *
 * @n: The integer to be found
 *
 * Return: The absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
