#include "main.h"
#include <string.h>
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - Checks if the input is a digit
 *
 * @c: The value to be checked
 *
 * Return: 1 if c is digit, 0 if not
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
