/* Takeint as a parameter and update to 98 */
void reset_to_98(int *n);

/* Swop the value of two integers */
void swap_int(int *a, int *b);

/* Return the length of a string */
int _strlen(char *s);

/* Print a string to stdout */
void _puts(char *str);

/* Print a string in reverse */
void print_rev(char *s);

/* Reverse a string */
void rev_string(char *s);

/* Print every other character starting with the first character */
void puts2(char *str);

/* Print half a string */
void puts_half(char *str);

/* Print n elements of an array of integers */
void print_array(int *a, int n);

/* copy the string pointed by src */
char *_strcpy(char *dest, char *src);

/* putchar */
int _putchar (char c);
